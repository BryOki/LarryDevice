# Larry Landing Page

Standalone static landing page for Larry.

Open `index.html` in a browser, or serve this folder locally:

```sh
python3 -m http.server 5174 --bind 127.0.0.1
```

The waitlist form validates email addresses and uses a placeholder submit function in `script.js`.
